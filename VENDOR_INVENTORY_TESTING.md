# اختبار إصلاح مشكلة الكمية في المخزن

## خطوات الاختبار

### 1. اختبار إضافة منتج جديد

#### الخطوات:
1. سجل دخول كتاجر
2. اذهب إلى صفحة المنتجات
3. اضغط "إضافة منتج جديد"
4. املأ البيانات:
   - اسم المنتج: "منتج تجريبي 1"
   - SKU: سيتم توليده تلقائياً
   - السعر: 100
   - **الكمية في المخزن: 50**
   - الحالة: نشط
5. اضغط "حفظ المنتج"

#### النتيجة المتوقعة:
- ✅ يتم حفظ المنتج بنجاح
- ✅ رسالة نجاح تظهر
- ✅ يتم التوجيه لصفحة تعديل المنتج
- ✅ حقل الكمية يعرض 50

#### التحقق من قاعدة البيانات:
```sql
-- استبدل PRODUCT_ID بـ ID المنتج الذي تم إنشاؤه
SELECT * FROM product_inventories WHERE product_id = PRODUCT_ID;
```

**يجب أن تظهر:**
- qty = 50
- inventory_source_id = 1
- vendor_id = [ID التاجر]

---

### 2. اختبار تعديل الكمية

#### الخطوات:
1. افتح المنتج الذي تم إنشاؤه
2. غير الكمية من 50 إلى 100
3. اضغط "حفظ المنتج"

#### النتيجة المتوقعة:
- ✅ يتم تحديث الكمية بنجاح
- ✅ رسالة نجاح تظهر
- ✅ حقل الكمية يعرض 100

#### التحقق من قاعدة البيانات:
```sql
SELECT * FROM product_inventories WHERE product_id = PRODUCT_ID;
```

**يجب أن تظهر:**
- qty = 100

---

### 3. اختبار ظهور المنتج في الموقع

#### الخطوات:
1. اذهب إلى الصفحة الرئيسية للموقع
2. ابحث عن المنتج أو اذهب لصفحة المنتجات

#### النتيجة المتوقعة:
- ✅ المنتج يظهر في القائمة
- ✅ زر "أضف للسلة" متاح
- ✅ لا توجد رسالة "out of stock"

---

### 4. اختبار المنتج بكمية صفر

#### الخطوات:
1. افتح المنتج
2. غير الكمية إلى 0
3. اضغط "حفظ المنتج"
4. اذهب للموقع

#### النتيجة المتوقعة:
- ✅ المنتج لا يظهر في القائمة (أو يظهر كـ out of stock)
- ✅ زر "أضف للسلة" غير متاح

---

### 5. اختبار المنتج غير النشط

#### الخطوات:
1. افتح المنتج
2. غير الحالة إلى "غير نشط"
3. اضغط "حفظ المنتج"
4. اذهب للموقع

#### النتيجة المتوقعة:
- ✅ المنتج لا يظهر في الموقع نهائياً

---

## استعلامات SQL مفيدة

### عرض جميع المنتجات مع الكمية
```sql
SELECT 
    p.id,
    p.name,
    p.sku,
    p.status,
    p.vendor_id,
    pi.qty,
    pi.inventory_source_id
FROM products p
LEFT JOIN product_inventories pi ON p.id = pi.product_id
WHERE p.vendor_id = [VENDOR_ID]
ORDER BY p.created_at DESC;
```

### عرض المنتجات بدون كمية
```sql
SELECT 
    p.id,
    p.name,
    p.sku,
    p.vendor_id
FROM products p
LEFT JOIN product_inventories pi ON p.id = pi.product_id
WHERE p.vendor_id = [VENDOR_ID]
AND (pi.id IS NULL OR pi.qty = 0);
```

### تحديث الكمية يدوياً (للاختبار فقط)
```sql
UPDATE product_inventories 
SET qty = 100 
WHERE product_id = PRODUCT_ID 
AND inventory_source_id = 1;
```

---

## المشاكل المحتملة وحلولها

### المشكلة: الكمية لا تحفظ
**الحل:**
1. تأكد من أن حقل الكمية في الفورم اسمه `inventories[1]`
2. تأكد من أن الـ Controller يستدعي `update` بعد `create`
3. تحقق من أن جدول `product_inventories` يحتوي على الحقول المطلوبة

### المشكلة: المنتج لا يظهر في الموقع
**الحل:**
1. تأكد من أن `status = 1`
2. تأكد من أن `qty > 0`
3. تأكد من أن المنتج مرتبط بـ channel نشط
4. تأكد من أن المنتج له صورة واحدة على الأقل

### المشكلة: خطأ عند حفظ المنتج
**الحل:**
1. تحقق من الـ logs في `storage/logs/laravel.log`
2. تأكد من أن جميع الحقول المطلوبة مملوءة
3. تأكد من أن الـ vendor_id موجود في جدول vendors

---

## ملاحظات
- الـ inventory_source_id الافتراضي هو 1
- يمكن للتاجر إضافة منتجات بكمية 0 لكنها لن تظهر في الموقع
- عند تعديل الكمية، يتم تحديث السجل الموجود وليس إنشاء سجل جديد
